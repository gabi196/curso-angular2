var myVAr = 'my var';

function myFunc(x, y){
    return x + y;
}

let num = 12;
const PI = 3.14;

var numeros = [1, 2, 3];
numeros.map(valor => valor*2);

class Soma {
    soma(x,y){
        return x + y; 
    }
}

var n1= 'sfds'; //Eh possivel tipar a variavel